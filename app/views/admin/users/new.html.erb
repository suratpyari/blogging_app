<div id="new_form">
  <h3> Add a new user </h3>
  <% form_for [:admin,@user], :html => {:id => "user_form", :multipart => true} do |f|%>
    <%= render :partial=> 'form' %>
    <div class ="divitem"><%= submit_tag "Add" %></div>
  <% end %>
</div>
